# 生命教育协作平台 - 部署指南

## 部署方案（Render + Cloudinary）

- **代码托管**: GitHub
- **应用部署**: Render
- **文件存储**: Cloudinary

---

## 前置准备

### 1. 注册账号

| 服务 | 注册地址 | 说明 |
|------|---------|------|
| GitHub | https://github.com | 存放代码 |
| Render | https://render.com | 部署应用（需要绑定银行卡验证身份）|
| Cloudinary | https://cloudinary.com | 存储用户上传的文件 |

---

## 部署步骤

### 第一步：推送到 GitHub

1. 在 GitHub 创建新仓库（建议设为 Private）
2. 在项目根目录执行：

```bash
cd d:\edu_platform
git init
git add .
git commit -m "初始提交"
git branch -M main
git remote add origin https://github.com/你的用户名/你的仓库名.git
git push -u origin main
```

---

### 第二步：配置 Cloudinary

1. 登录 Cloudinary 账号
2. 进入 Dashboard，获取以下信息：
   - `Cloud Name`
   - `API Key`
   - `API Secret`

---

### 第三步：部署到 Render

#### 3.1 创建 Web Service

1. 登录 Render
2. 点击 **New** → **Web Service**
3. 选择刚刚推送到 GitHub 的仓库
4. 配置以下信息：

| 配置项 | 值 |
|-------|-----|
| Name | edu-platform（或你喜欢的名字）|
| Region | Singapore (新加坡) - 国内访问速度较快 |
| Branch | main |
| Runtime | Python 3.12 (runtime.txt 指定) |
| Build Command | `pip install -r requirement.txt && python edu_platform/manage.py collectstatic --noinput` |
| Start Command | `gunicorn edu_platform.wsgi --log-file -` |

> **注意**: 项目根目录的 `runtime.txt` 文件会自动指定 Python 版本为 3.12.3

#### 3.2 添加环境变量

点击 **Advanced** → **Add Environment Variable**：

| 环境变量 | 值 |
|---------|-----|
| SECRET_KEY | 生成一个强密钥（推荐使用 50+ 位随机字符串）|
| DEBUG | False |
| ALLOWED_HOSTS | .onrender.com |
| CLOUDINARY_URL | `cloudinary://API_KEY:API_SECRET@CLOUD_NAME` |
| CLOUDINARY_CLOUD_NAME | 你的 Cloud Name |
| CLOUDINARY_API_KEY | 你的 API Key |
| CLOUDINARY_API_SECRET | 你的 API Secret |

#### 3.3 创建数据库

1. 在 Render Dashboard 点击 **New** → **PostgreSQL**
2. 创建数据库后，Render 会自动添加 `DATABASE_URL` 环境变量到你的 Web Service

#### 3.4 部署应用

点击 **Create Web Service**，Render 会自动部署应用。

---

### 第四步：完成配置

1. 部署完成后，访问应用网址：`https://your-app-name.onrender.com`
2. 创建超级管理员账号（在 Render 的 Shell 中执行）：

```bash
python edu_platform/manage.py createsuperuser
```

---

## 访问应用

部署完成后，Render 会给你一个网址，类似：
`https://your-app-name.onrender.com`

| 页面 | 地址 |
|------|------|
| 首页 | `/` |
| 登录 | `/login/` |
| 注册 | `/register/` |
| 管理员后台 | `/admin/` |

---

## 本地开发说明

本地开发时：
- 使用 SQLite 数据库（无需配置 PostgreSQL）
- 文件上传到 `media/` 目录（无需 Cloudinary）
- 运行：`python manage.py runserver`

### 本地运行步骤

```bash
cd d:\edu_platform\edu_platform
python manage.py migrate
python manage.py createsuperuser
python manage.py runserver
```

---

## 文件说明

| 文件 | 说明 |
|------|------|
| `requirement.txt` | 依赖列表 |
| `Procfile` | Render 启动脚本 |
| `.gitignore` | Git 忽略文件 |
| `.env.example` | 环境变量示例 |
| `deploy_guide.md` | 部署指南 |

---

## 常用命令

| 命令 | 说明 |
|------|------|
| `python manage.py migrate` | 运行数据库迁移 |
| `python manage.py createsuperuser` | 创建超级管理员 |
| `python manage.py collectstatic` | 收集静态文件 |
| `git push origin main` | 推送代码到 GitHub |

---

## 注意事项

1. **不要提交敏感信息到 GitHub**：确保 `.env` 文件在 `.gitignore` 中
2. **生成强密钥**：不要使用默认的 `SECRET_KEY`，可以用以下命令生成：
   ```bash
   python -c "import secrets; print(secrets.token_hex(50))"
   ```
3. **定期备份数据库**：Render 提供自动备份，但建议定期手动备份
4. **Cloudinary 免费额度**：免费版提供 25GB 带宽/月，足够个人项目使用
5. **Render 免费版限制**：免费版应用会在 15 分钟无活动后休眠，首次访问需要约 30 秒唤醒

---

## 功能完整性

✅ 所有功能均已适配：
- 用户注册/登录/退出
- 角色权限管理（学生、教师、助教、管理员）
- 课程资源管理（上传到 Cloudinary）
- 活动管理
- 协作讨论
- 文件上传下载

---

## 技术栈

| 组件 | 版本 |
|------|------|
| Django | 6.0.2 |
| Gunicorn | 21.2.0 |
| PostgreSQL | 15+ (Render) |
| Cloudinary | 1.36.0 |
| WhiteNoise | 6.5.0 |
