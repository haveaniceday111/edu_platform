# 生命教育协作平台 - 部署指南

## 部署方案（PythonAnywhere）

- **代码托管**: GitHub
- **应用部署**: PythonAnywhere
- **文件存储**: PythonAnywhere 本地存储

---

## 前置准备

### 1. 注册账号

| 服务 | 注册地址 | 说明 |
|------|---------|------|
| GitHub | https://github.com | 存放代码 |
| PythonAnywhere | https://www.pythonanywhere.com | 部署应用（需要付费版 $5/月） |

---

## 部署步骤

### 第一步：推送到 GitHub

1. 在 GitHub 创建新仓库
2. 在项目根目录执行：

```bash
cd d:\edu_platform
git init
git add .
git commit -m "初始提交"
git branch -M main
git remote add origin https://github.com/你的用户名/你的仓库名.git
git push -u origin main
```

---

### 第二步：配置 PythonAnywhere

#### 2.1 创建 Web App

1. 登录 PythonAnywhere
2. 点击 **Web** → **Add a new web app**
3. 选择 **Django** → **Python 3.11**
4. 输入域名（如：`yourusername.pythonanywhere.com`）

#### 2.2 上传代码

1. 点击 **Consoles** → **Bash**
2. 进入项目目录：

```bash
cd /home/yourusername
git clone https://github.com/你的用户名/你的仓库名.git
```

#### 2.3 配置虚拟环境

```bash
cd your-repo-name
python -m venv venv
source venv/bin/activate
pip install -r requirement.txt
```

#### 2.4 配置 WSGI 文件

1. 回到 **Web** 页面
2. 点击 **WSGI configuration file**
3. 修改为：

```python
import os
import sys

path = '/home/yourusername/your-repo-name/edu_platform'
if path not in sys.path:
    sys.path.append(path)

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'edu_platform.settings')

from django.core.wsgi import get_wsgi_application
application = get_wsgi_application()
```

#### 2.5 设置静态文件

1. 在 **Static files** 部分添加：
   - URL: `/static/`
   - Directory: `/home/yourusername/your-repo-name/static`

#### 2.6 设置媒体文件

1. 在 **Files** 中创建 `media` 目录
2. 在 **Static files** 部分添加：
   - URL: `/media/`
   - Directory: `/home/yourusername/your-repo-name/media`

#### 2.7 配置数据库

1. 点击 **Databases** → **Add a new MySQL database**
2. 创建数据库后记录：
   - Database name: `yourusername$edu_platform`
   - Username: `yourusername`
   - Password: 自动生成

3. 在 `settings.py` 中配置：

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'yourusername$edu_platform',
        'USER': 'yourusername',
        'PASSWORD': 'your-db-password',
        'HOST': 'yourusername.mysql.pythonanywhere-services.com',
        'PORT': '3306',
    }
}
```

#### 2.8 设置环境变量

在 **Web** → **Environment variables** 中添加：

| 环境变量 | 值 |
|---------|-----|
| SECRET_KEY | 生成一个强密钥 |
| DEBUG | False |
| ALLOWED_HOSTS | yourusername.pythonanywhere.com |

#### 2.9 运行迁移

```bash
cd /home/yourusername/your-repo-name
source venv/bin/activate
cd edu_platform
python manage.py migrate
python manage.py createsuperuser
```

#### 2.10 重启应用

点击 **Web** → **Reload** 重启应用

---

## 访问应用

部署完成后，访问地址：
`https://yourusername.pythonanywhere.com`

---

## 本地开发说明

本地开发时：
- 使用 SQLite 数据库
- 文件上传到 `media/` 目录
- 运行：`python manage.py runserver`

---

## 文件说明

| 文件 | 说明 |
|------|------|
| `requirement.txt` | 依赖列表 |
| `.gitignore` | Git 忽略文件 |

---

## 常用命令

| 命令 | 说明 |
|------|------|
| `python manage.py migrate` | 运行数据库迁移 |
| `python manage.py createsuperuser` | 创建超级管理员 |
| `python manage.py collectstatic` | 收集静态文件 |

---

## 注意事项

1. **不要**提交敏感信息到 GitHub
2. 不要使用默认的 `SECRET_KEY`，生成新的
3. 定期备份数据库
4. 付费版每月 $5，注意续费

---

## 代码修改说明

使用 PythonAnywhere 部署**基本不需要修改代码**！

唯一需要调整的是 `settings.py` 中的数据库配置，建议使用环境变量：

```python
import os

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': os.environ.get('DB_NAME', 'edu_platform'),
        'USER': os.environ.get('DB_USER', 'root'),
        'PASSWORD': os.environ.get('DB_PASSWORD', ''),
        'HOST': os.environ.get('DB_HOST', 'localhost'),
        'PORT': os.environ.get('DB_PORT', '3306'),
    }
}
```
